apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: admin-api-route
  namespace: profertility
spec:
  parentRefs:
    - name: profertility-gateway
  hostnames:
    - "admin.mojasim.tech"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: profertility-admin
          port: 8080